clc; clear;
test = zeros(3,4);
test(1,1)

array = zeros(10,10);
array(2,1) = 1;
[res_m, res_n] = find(array);
fprintf("res = %d, %d", res_m, res_n);



function array = Enqueue(array, elem)
    array = [array elem];
end

function array = Dequeue(array)
    [m, array_elements] = size(array);
    if array_elements < 1
       return;
    end
    array(1) = [];
end